const notas_router = require('./notas.js');
const login_router = require('./login.js');
const usuarios_router = require('./usuarios.js');
const session_router = require('./session.js');
module.exports = {
    notas_router,
    login_router,
    usuarios_router,
    session_router
}